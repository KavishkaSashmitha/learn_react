import { NextPage } from 'next';
import TaskManager from '../components/TaskManagerApp';

const Page: NextPage = ({}) => {
  return (
    <div>
      <TaskManager />
    </div>
  );
};

export default Page;
